# 🚀 智能药箱项目（ESP-IDF）开发环境启动与编译指南

本指南用于解决 VS Code 插件路径失联问题，通过 Windows CMD 终端实现 **一键激活环境、项目跳转、编译与烧录**。

## 📥 方式一：一键批处理脚本（最推荐，秒进状态）

为了避免每次手动复制长路径，可在电脑桌面制作一个双击即用的快捷脚本：

1. 在电脑桌面空白处点击右键，新建一个 **文本文档**。
2. 将文档名称连带后缀改为：`启动智能药箱开发环境.bat` （**注意：** 必须确保后缀是 `.bat`，而不是 `.txt`）。
3. 鼠标右键点击该文件选择“编辑”（或用记事本打开），将以下代码复制进去并保存：

DOS

```c
@echo off
echo ==========================================
echo 正在为您激活 ESP-IDF v5.1.2 开发环境...
echo ==========================================
:: 【注意】此处路径需根据自己电脑上 ESP-IDF 的实际安装位置进行修改
call D:\Embedded_Systems_app_install\programs_2026\Espressif\frameworks\esp-idf-v5.1.2\export.bat

echo ==========================================
echo 正在跳转到智能药箱 FreeRTOS 项目目录...
echo ==========================================
:: 【注意】此处路径需根据自己智能药箱项目的实际存放位置进行修改
cd /d D:\Embedded_Systems_app_install\Academic_Project\Embedded_CodeSmartMedBox\Esp_SmartMedBox_FreeRTOS

cmd /k echo 环境已就绪！直接输入 idf.py build 即可开始编译。
```

1. **使用方法**：以后每次开发前，直接**双击运行**这个 `.bat` 文件，它会自动完成环境配置并帮你切换到项目根目录。

## 💻 方式二：手动分步执行（标准流程）

如果你不想创建脚本，也可以手动打开普通的 Windows CMD 终端，依次输入以下命令：

### 1. 激活 ESP-IDF 环境变量

打开 `cmd`，复制并运行以下命令（路径需根据实际安装位置微调）：

DOS

```
D:\Embedded_Systems_app_install\programs_2026\Espressif\frameworks\esp-idf-v5.1.2\export.bat
```

> 💡 **成功标志**：等待终端检测运行完毕，最后一行输出 `Done! You can now compile ESP-IDF projects.` 即代表激活成功。

### 2. 跳转到项目根目录

DOS

```c
cd /d D:\Embedded_Systems_app_install\Academic_Project\Embedded_CodeSmartMedBox\Esp_SmartMedBox_FreeRTOS

输入code .  进入vs code
```

## 🛠️ 高频开发命令（进入项目目录后使用）

当终端环境激活且路径正确后，你可以在该窗口中直接运行以下命令，或者此时**直接在 VS Code 中点击底部的图形化图标**进行操作：

- **清理旧缓存（换芯片配置或编译报错卡死时使用）**：

	DOS

	```
	rd /s /q build
	idf.py set-target esp32s3
	```

- **编译项目**：

	DOS

	```
	idf.py build
	```

- **烧录固件（`COM18` 需根据设备在设备管理器中的实际串口号进行修改）**：

	DOS

	```
	idf.py -p COM18 flash
	```

- **烧录 + 自动打开串口监视器（一步到位连招，查看 FreeRTOS 任务及 Log 输出）**：

	DOS

	```
	idf.py -p COM18 flash monitor
	```

	*(提示：退出串口监视器请按下键盘组合键 `Ctrl + ]`)*